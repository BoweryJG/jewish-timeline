{
  "name": "jewish-timeline-image-service",
  "version": "1.0.0",
  "description": "Unlimited image generation service for Jewish Timeline",
  "main": "generate-with-replicate.js",
  "scripts": {
    "generate": "node generate-with-replicate.js",
    "generate:single": "node generate-with-replicate.js --single",
    "generate:local": "node generate-local-flux.js",
    "setup": "npm install && cp .env.example .env"
  },
  "dependencies": {
    "replicate": "^0.25.1",
    "dotenv": "^16.3.1",
    "@supabase/supabase-js": "^2.39.0",
    "bull": "^4.12.0",
    "sharp": "^0.33.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}